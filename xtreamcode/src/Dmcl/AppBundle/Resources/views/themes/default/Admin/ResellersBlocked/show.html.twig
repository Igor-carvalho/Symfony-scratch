{% extends '::base.html.twig' %}

{% block body -%}
    <h1>ResellersBlocked</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Reseller</th>
                <td>{{ entity.reseller }}</td>
            </tr>
            <tr>
                <th>Reason</th>
                <td>{{ entity.reason }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('resellers-blocked') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('resellers-blocked_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
